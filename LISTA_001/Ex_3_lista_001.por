programa {
  funcao inicio() {
    real b, al, area
    
    escreva("Digite a base e altura de um triangulo: ")
    leia(b, al)
    area = (b * al) * 2
    limpa()
    escreva("Area = ", area)
  }
}
