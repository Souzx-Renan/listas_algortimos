programa {
  funcao inicio() {
    real l, b, c
    inteiro z

    escreva("Digite valores para L, B e C: ")
    leia(l, b, c)
    limpa()
    
    z = (l + b + c) * 2
    
    escreva("Resultado da soma multiplicado por 2 = ", z)
  }
}
